#include <stdlib.h>

// Combine the trees of the given elements.
size_t forest_union(size_t element_a, size_t element_b, size_t nodes, size_t forest[nodes]);
// Find root of the given element.
size_t find(size_t index, size_t nodes, size_t forest[nodes]);